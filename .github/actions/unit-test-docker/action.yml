name: 'Unit Test on Docker'
description: 'Run Unit Tests for the specified project'  

inputs:
  path-to-docker-file:
    description: 'path to docker file'
    required: true
runs:
  using: 'docker'
  image: 'microservices/platform/test/Platform.Test/Dockerfile'
